import { createTheme } from '@mui/material'

export const MUI_THEME = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#3e29c0'
    },
    secondary: {
      main: '#a71cb2'
    }
  }
})
